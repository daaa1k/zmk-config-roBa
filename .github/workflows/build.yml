on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    permissions:
      actions: write
      contents: read
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@edf5c0814fd3ea202e43aad2d68fd32e882a518c # v0.3.0
