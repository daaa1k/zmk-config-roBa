name: "Draw Keymap"

on:
  workflow_dispatch:
  push:
    paths:
      - "config/roBa.keymap"
      - "keymap_drawer.config.yaml"
      - "config/roBa.dtsi"

jobs:
  draw:
    uses: daaa1k/keymap-drawer/.github/workflows/draw-zmk.yml@7775ba4081f512101972fd696ac80120af150417
    permissions:
      contents: write
    with:
      commit_message: "docs: update keymap drawing"
      amend_commit: false
      keymap_patterns: "config/*.keymap"
      json_path: "config"
      config_path: "keymap_drawer.config.yaml" # config file, ignored if not exists
      output_folder: "keymap-drawer"
      destination: "both"
